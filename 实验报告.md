# 考勤管理系统实验报告

## 目录

1. [需求分析](#1-需求分析)
   - 1.1 问题提出
   - 1.2 系统分析
   - 1.3 功能需求
   - 1.4 非功能性需求
   - 1.5 开发与运行环境

2. [数据库设计与实现](#2-数据库设计与实现)
   - 2.1 概念设计
   - 2.2 逻辑设计与物理设计
   - 2.3 数据库实现

3. [应用程序设计](#3-应用程序设计)
   - 3.1 系统功能设计
   - 3.2 系统详细设计
   - 3.3 小结

---

## 1. 需求分析

### 1.1 问题提出

随着企业规模的扩大和管理规范化需求的提升，传统的人工考勤管理方式已无法满足现代企业的管理需求。考勤管理系统旨在为企业提供一套完整的数字化考勤解决方案。

**应用环境**：本系统适用于中小型企业的日常考勤管理，支持多部门、多职务的灵活配置，能够适应不同岗位的工作时间要求。

**用户群体**：系统面向三类用户：普通员工（EMPLOYEE）、审批人（APPROVER）和管理员（ADMIN）。普通员工可进行打卡、申请和查看个人统计；审批人负责审批待处理的申请；管理员拥有完整的系统管理权限，包括员工、部门、职务的增删改查以及考勤汇总查看。

**必要性和意义**：传统考勤方式存在记录不准确、统计效率低、数据易丢失等问题。本系统通过数字化手段实现考勤数据的自动记录、实时统计和规范管理，提高考勤管理效率，减少人工错误，为企业人力资源管理提供数据支持，同时为员工提供便捷的打卡和申请渠道，提升工作体验。

### 1.2 系统分析

#### 1.2.1 数据流图

```
┌─────────┐         ┌──────────────┐         ┌──────────┐
│  员工   │────────>│  打卡模块    │────────>│ 考勤记录 │
└─────────┘         └──────────────┘         └──────────┘
     │                      │                       │
     │                      │                       │
     v                      v                       v
┌─────────┐         ┌──────────────┐         ┌──────────┐
│  申请   │────────>│  审批模块    │────────>│ 申请记录 │
└─────────┘         └──────────────┘         └──────────┘
     │                      │                       │
     │                      │                       │
     v                      v                       v
┌─────────┐         ┌──────────────┐         ┌──────────┐
│ 管理员  │────────>│  管理模块    │────────>│ 基础数据 │
└─────────┘         └──────────────┘         └──────────┘
```

#### 1.2.2 数据字典

**1. 部门表（department）**
- DID：部门ID，CHAR(10)，主键，格式：D000000001
- DName：部门名称，VARCHAR(30)，非空
- Description：部门描述，VARCHAR(200)
- ManagerID：负责人ID，INT，外键引用employee(EID)

**2. 职务配置表（position_config）**
- PID：职务ID，CHAR(10)，主键，格式：P000000001
- PName：职务名称，VARCHAR(20)，非空
- WorkStartTime：标准上班时间，TIME，非空
- WorkEndTime：标准下班时间，TIME，非空
- MonthlyWorkDays：月工作日，INT，默认21
- DailyWorkHours：每日工作时长，DECIMAL(3,1)，默认8.0
- Description：职务描述，VARCHAR(200)

**3. 员工表（employee）**
- EID：员工ID，INT，主键，自增
- DID：部门ID，CHAR(10)，外键，非空
- PID：职务ID，CHAR(10)，外键，非空
- EName：员工姓名，CHAR(20)，非空
- Sex：性别，CHAR(2)，取值：'男'/'女'
- Phone：联系电话，CHAR(11)，唯一
- Password：登录密码，CHAR(20)，非空
- Role：用户角色，CHAR(10)，默认'EMPLOYEE'，取值：EMPLOYEE/APPROVER/ADMIN

**4. 考勤记录表（attendance_record）**
- AID：考勤记录ID，INT，主键，自增
- EID：员工ID，INT，外键，非空
- RecordDate：考勤日期，DATE，非空
- CheckInTime：签到时间，DATETIME
- CheckInLocation：签到地点，VARCHAR(100)
- CheckOutTime：签退时间，DATETIME
- CheckOutLocation：签退地点，VARCHAR(100)
- Status：考勤状态，VARCHAR(20)，默认'NORMAL'，取值：NORMAL/LATE/EARLY_LEAVE/MISSING
- WorkHours：工作时长，DECIMAL(4,2)，单位：小时

**5. 申请表（application）**
- AID：申请ID，INT，主键，自增
- EID：申请人ID，INT，外键，非空
- ApplicationType：申请类型，VARCHAR(20)，非空，取值：LEAVE/REISSUE/OVERTIME/BUSINESS_TRIP
- StartTime：开始时间，DATETIME，非空
- EndTime：结束时间，DATETIME，非空
- Reason：申请原因，TEXT，非空
- Status：申请状态，VARCHAR(20)，默认'PENDING'，取值：PENDING/APPROVED/REJECTED/CANCELLED
- ApproverID：审批人ID，INT，外键
- ApproveTime：审批时间，DATETIME
- RejectReason：拒绝原因，TEXT
- CreatedAt：创建时间，DATETIME，默认当前时间
- LeaveType：请假类型，VARCHAR(20)（请假申请特有）
- Attachment：附件路径，VARCHAR(500)（请假申请特有）
- ReissueType：补卡类型，VARCHAR(20)（补卡申请特有）
- ReissueTime：补卡时间，DATETIME（补卡申请特有）
- OvertimeType：加班类型，VARCHAR(20)（加班申请特有）
- IsCompensatory：是否调休，BOOLEAN，默认FALSE（加班申请特有）
- Destination：目的地，VARCHAR(100)（出差申请特有）
- Companions：随行人员，VARCHAR(500)，JSON格式（出差申请特有）
- Transportation：交通工具，VARCHAR(50)（出差申请特有）
- Budget：预算金额，DECIMAL(10,2)（出差申请特有）

### 1.3 功能需求

#### 1.3.1 功能结构图

```
考勤管理系统
├── 用户认证模块
│   ├── 用户登录
│   └── 用户注册
│
├── 员工功能模块
│   ├── 打卡功能
│   │   ├── 上班打卡
│   │   └── 下班打卡
│   ├── 申请管理
│   │   ├── 请假申请
│   │   ├── 加班申请
│   │   ├── 出差申请
│   │   └── 补卡申请
│   ├── 统计汇总
│   │   ├── 周度统计
│   │   └── 月度统计
│   └── 个人中心
│       ├── 修改个人信息
│       └── 修改密码
│
├── 审批人功能模块
│   ├── 打卡功能
│   ├── 审批中心
│   │   ├── 查看待审批申请
│   │   ├── 审批通过
│   │   └── 审批驳回
│   └── 个人中心
│
└── 管理员功能模块
    ├── 打卡功能
    ├── 员工管理
    │   ├── 员工列表查询
    │   ├── 新增员工
    │   ├── 编辑员工
    │   ├── 删除员工
    │   └── 重置密码
    ├── 部门管理
    │   ├── 部门列表查询
    │   ├── 新增部门
    │   ├── 编辑部门
    │   └── 删除部门
    ├── 职务管理
    │   ├── 职务列表查询
    │   ├── 新增职务
    │   ├── 编辑职务
    │   └── 删除职务
    ├── 考勤汇总
    │   ├── 按部门查询
    │   ├── 按员工查询
    │   └── 按日期范围查询
    ├── 审批管理
    │   ├── 查看所有申请
    │   ├── 审批通过
    │   └── 审批驳回
    └── 个人中心
```

#### 1.3.2 各主要功能说明

**1. 打卡功能**
- **上班打卡**：员工在标准上班时间前后30分钟内进行打卡，系统自动记录打卡时间和GPS位置，并根据打卡时间判断是否迟到。
- **下班打卡**：员工在标准下班时间前后30分钟内进行打卡，系统自动计算工作时长，并根据签退时间判断是否早退。
- **打卡限制**：必须先完成上班打卡才能进行下班打卡；每个员工每天只能有一条考勤记录。

**2. 申请管理**
- **请假申请**：支持病假、年假、事假、婚假、产假等多种请假类型，需填写开始时间、结束时间、理由，可上传附件。
- **加班申请**：支持平日加班、周末加班、节假日加班，可选择是否调休，需填写开始时间、结束时间、理由。
- **出差申请**：需填写出差地点、随行人员、交通工具、费用预算、开始时间、结束时间、理由。
- **补卡申请**：支持补上班打卡和补下班打卡，补卡时间不能晚于当前时间，每月最多3次补卡机会。
- **申请限制**：
  - 补卡申请：每月最多3次；补卡时间不能晚于当前时间；不能与正常考勤日期重复；同一天不能重复提交补卡申请。
  - 其他申请：开始时间不能早于当前时间；结束时间必须晚于开始时间；申请时间段不能与其他已通过或待审批的申请重叠。

**3. 审批功能**
- **审批人审批**：审批人可以查看待审批的申请，进行通过或驳回操作，驳回时需填写拒绝原因。
- **管理员审批**：管理员可以查看所有申请并进行审批操作。
- **审批限制**：只能审批状态为"待审批"的申请；审批后申请状态自动更新。

**4. 统计汇总**
- **个人统计**：员工可查看自己的周度/月度考勤统计，包括正常天数、迟到天数、早退天数、缺卡天数、总工作时长等。
- **管理员统计**：管理员可按部门、员工、日期范围、状态筛选查看所有员工的考勤记录和统计信息。

**5. 管理员功能**
- **员工管理**：
  - 新增：必须填写姓名、部门ID、职务ID；手机号必须唯一（可为空）；默认密码为"123456"；默认角色为"EMPLOYEE"。
  - 编辑：可修改姓名、手机号、部门、职务、角色；不能修改密码（需通过重置密码功能）。
  - 删除：删除员工会级联删除该员工的所有考勤记录和申请记录。
  - 重置密码：将员工密码重置为默认密码"123456"。
- **部门管理**：
  - 新增：自动生成部门ID（格式：D000000001）；必须填写部门名称。
  - 编辑：可修改部门名称、描述、负责人。
  - 删除：如果部门下还有员工，则不能删除（外键约束：ON DELETE RESTRICT）。
- **职务管理**：
  - 新增：自动生成职务ID（格式：P000000001）；必须填写职务名称、上班时间、下班时间；默认月工作日21天，每日工作时长8小时。
  - 编辑：可修改所有字段。
  - 删除：如果职务下还有员工，则不能删除（外键约束：ON DELETE RESTRICT）。

### 1.4 非功能性需求

**1. 性能需求**
- 系统响应时间：普通查询操作响应时间应小于1秒，复杂统计查询响应时间应小于3秒。
- 并发支持：系统应支持至少50个用户同时在线使用。
- 数据容量：系统应支持至少1000名员工、100个部门的规模。

**2. 可靠性需求**
- 系统可用性：系统应保证99%以上的可用性。
- 数据完整性：通过外键约束、唯一约束、非空约束等保证数据完整性。
- 错误处理：系统应具备完善的异常处理机制，避免因异常导致系统崩溃。

**3. 安全性需求**
- 用户认证：所有用户必须通过账号密码登录才能使用系统。
- 权限控制：基于角色的访问控制（RBAC），不同角色拥有不同的功能权限。
- 数据安全：密码采用明文存储（实际应用中应使用加密存储），敏感操作需要权限验证。

**4. 可维护性需求**
- 代码规范：遵循Java编码规范，代码结构清晰，注释完整。
- 模块化设计：采用分层架构，Controller-Service-DAO三层分离，便于维护和扩展。
- 日志记录：关键操作记录日志，便于问题排查。

**5. 可扩展性需求**
- 系统架构：前后端分离架构，便于独立扩展和维护。
- 数据库设计：合理的表结构和索引设计，支持未来功能扩展。
- 接口设计：RESTful API设计，便于与其他系统集成。

### 1.5 开发与运行环境

**开发环境**
- **操作系统**：Windows 10/11
- **开发工具**：IntelliJ IDEA / Visual Studio Code
- **后端框架**：Spring Boot 3.3.0
- **前端框架**：React 18.3.1 + TypeScript 5.5.4
- **构建工具**：Maven 3.8+ / Vite 5.4.8
- **数据库**：MySQL 8.0+
- **JDK版本**：Java 17
- **Node.js版本**：Node.js 18+

**运行环境**
- **服务器**：Spring Boot内嵌Tomcat服务器
- **数据库**：MySQL 8.0+
- **浏览器**：Chrome、Firefox、Edge等现代浏览器
- **网络**：支持HTTP/HTTPS协议

**技术栈**
- **后端**：Spring Boot、Spring JDBC、Spring Security、MySQL Connector
- **前端**：React、TypeScript、React Router、Axios、CSS3
- **数据库**：MySQL 8.0+

---

## 2. 数据库设计与实现

### 2.1 概念设计

#### 2.1.1 ER图

```
┌──────────────┐         ┌──────────────┐
│   Department │         │PositionConfig│
│              │         │              │
│ DID (PK)     │         │ PID (PK)      │
│ DName        │         │ PName         │
│ Description  │         │ WorkStartTime │
│ ManagerID    │         │ WorkEndTime   │
└──────┬───────┘         └──────┬───────┘
       │                        │
       │                        │
       │ 1                      │ 1
       │                        │
       │ N                      │ N
       │                        │
┌──────▼───────┐               │
│   Employee   │               │
│              │               │
│ EID (PK)     │───────────────┘
│ DID (FK)     │
│ PID (FK)     │
│ EName        │
│ Sex          │
│ Phone        │
│ Password     │
│ Role         │
└──────┬───────┘
       │
       │ 1
       │
       │ N
       │
┌──────▼──────────────┐    ┌──────────────┐
│ AttendanceRecord    │    │ Application  │
│                     │    │              │
│ AID (PK)            │    │ AID (PK)      │
│ EID (FK)            │    │ EID (FK)      │
│ RecordDate          │    │ ApplicationType│
│ CheckInTime         │    │ StartTime     │
│ CheckInLocation     │    │ EndTime       │
│ CheckOutTime        │    │ Reason        │
│ CheckOutLocation    │    │ Status        │
│ Status              │    │ ApproverID(FK)│
│ WorkHours           │    │ ApproveTime  │
└─────────────────────┘    └──────────────┘
```

#### 2.1.2 ER图说明

**实体说明**：
1. **Department（部门）**：企业组织架构中的部门实体，包含部门ID、名称、描述和负责人信息。
2. **PositionConfig（职务配置）**：定义不同职务的工作时间标准，包含职务ID、名称、上下班时间、月工作日和每日工作时长。
3. **Employee（员工）**：系统用户实体，包含员工基本信息、所属部门、职务、登录凭证和角色权限。
4. **AttendanceRecord（考勤记录）**：记录员工每日的打卡信息，包含打卡时间、地点、状态和工作时长。
5. **Application（申请）**：员工提交的各种申请记录，包含申请类型、时间、原因、状态和审批信息。

**关系说明**：
1. **Department ↔ Employee**：一对多关系，一个部门可以有多个员工，一个员工只能属于一个部门。
2. **PositionConfig ↔ Employee**：一对多关系，一个职务配置可以对应多个员工，一个员工只能有一个职务。
3. **Employee ↔ AttendanceRecord**：一对多关系，一个员工可以有多条考勤记录，一条考勤记录只属于一个员工。
4. **Employee ↔ Application**：一对多关系，一个员工可以提交多个申请，一个申请只属于一个员工。
5. **Employee ↔ Application（审批关系）**：多对一关系，多个申请可以由同一个审批人审批，一个申请只能有一个审批人。

### 2.2 逻辑设计与物理设计

#### 2.2.1 关系数据库模式

**1. 部门表（department）**

```sql
CREATE TABLE department (
    DID CHAR(10) PRIMARY KEY COMMENT '部门ID，格式：D000000001',
    DName VARCHAR(30) NOT NULL COMMENT '部门名称',
    Description VARCHAR(200) COMMENT '部门描述',
    ManagerID INT COMMENT '负责人ID（员工ID）'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='部门信息表';
```

**2. 职务配置表（position_config）**

```sql
CREATE TABLE position_config (
    PID CHAR(10) PRIMARY KEY COMMENT '职务ID，格式：P000000001',
    PName VARCHAR(20) NOT NULL COMMENT '职务名称',
    WorkStartTime TIME NOT NULL COMMENT '标准上班时间（HH:MM:SS）',
    WorkEndTime TIME NOT NULL COMMENT '标准下班时间（HH:MM:SS）',
    MonthlyWorkDays INT DEFAULT 21 COMMENT '月工作日（天数）',
    DailyWorkHours DECIMAL(3,1) DEFAULT 8.0 COMMENT '每日工作时长（小时）',
    Description VARCHAR(200) COMMENT '职务描述'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='职务配置表';
```

**3. 员工表（employee）**

```sql
CREATE TABLE employee (
    EID INT AUTO_INCREMENT PRIMARY KEY COMMENT '员工ID（自增）',
    DID CHAR(10) NOT NULL COMMENT '部门ID',
    PID CHAR(10) NOT NULL COMMENT '职务ID',
    EName CHAR(20) NOT NULL COMMENT '员工姓名',
    Sex CHAR(2) CHECK (Sex IN ('男', '女')) COMMENT '性别',
    Phone CHAR(11) UNIQUE COMMENT '联系电话（唯一）',
    Password CHAR(20) NOT NULL COMMENT '登录密码',
    Role CHAR(10) DEFAULT 'EMPLOYEE' COMMENT '用户角色：EMPLOYEE-员工, APPROVER-审批人, ADMIN-管理员',
    FOREIGN KEY (DID) REFERENCES department(DID) ON DELETE RESTRICT ON UPDATE CASCADE,
    FOREIGN KEY (PID) REFERENCES position_config(PID) ON DELETE RESTRICT ON UPDATE CASCADE,
    INDEX idx_did (DID),
    INDEX idx_pid (PID),
    INDEX idx_role (Role)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='员工信息表';
```

**4. 考勤记录表（attendance_record）**

```sql
CREATE TABLE attendance_record (
    AID INT AUTO_INCREMENT PRIMARY KEY COMMENT '考勤记录ID（自增）',
    EID INT NOT NULL COMMENT '员工ID',
    RecordDate DATE NOT NULL COMMENT '考勤日期',
    CheckInTime DATETIME COMMENT '签到时间',
    CheckInLocation VARCHAR(100) COMMENT '签到地点',
    CheckOutTime DATETIME COMMENT '签退时间',
    CheckOutLocation VARCHAR(100) COMMENT '签退地点',
    Status VARCHAR(20) DEFAULT 'NORMAL' COMMENT '考勤状态：NORMAL-正常, LATE-迟到, EARLY_LEAVE-早退, MISSING-缺勤',
    WorkHours DECIMAL(4,2) COMMENT '工作时长（小时）',
    FOREIGN KEY (EID) REFERENCES employee(EID) ON DELETE CASCADE ON UPDATE CASCADE,
    INDEX idx_eid (EID),
    INDEX idx_date (RecordDate),
    UNIQUE KEY uk_employee_date (EID, RecordDate)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='考勤记录表';
```

**5. 申请表（application）**

```sql
CREATE TABLE application (
    AID INT AUTO_INCREMENT PRIMARY KEY COMMENT '申请ID（自增）',
    EID INT NOT NULL COMMENT '申请人ID（员工ID）',
    ApplicationType VARCHAR(20) NOT NULL COMMENT '申请类型：LEAVE-请假, REISSUE-补卡, OVERTIME-加班, BUSINESS_TRIP-出差',
    StartTime DATETIME NOT NULL COMMENT '开始时间',
    EndTime DATETIME NOT NULL COMMENT '结束时间',
    Reason TEXT NOT NULL COMMENT '申请原因',
    Status VARCHAR(20) DEFAULT 'PENDING' COMMENT '申请状态：PENDING-待审批, APPROVED-已批准, REJECTED-已拒绝, CANCELLED-已取消',
    ApproverID INT COMMENT '审批人ID',
    ApproveTime DATETIME COMMENT '审批时间',
    RejectReason TEXT COMMENT '拒绝原因',
    CreatedAt DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    -- 请假特有字段
    LeaveType VARCHAR(20) COMMENT '请假类型：ANNUAL-年假, SICK-病假, PERSONAL-事假, MARRIAGE-婚假, MATERNITY-产假, OTHER-其他',
    Attachment VARCHAR(500) COMMENT '附件路径',
    -- 补卡特有字段
    ReissueType VARCHAR(20) COMMENT '补卡类型：MISSING_CHECK_IN-漏签, MISSING_CHECK_OUT-漏退, BOTH-漏签漏退',
    ReissueTime DATETIME COMMENT '补卡时间',
    -- 加班特有字段
    OvertimeType VARCHAR(20) COMMENT '加班类型：WEEKDAY-工作日, WEEKEND-周末, HOLIDAY-节假日',
    IsCompensatory BOOLEAN DEFAULT FALSE COMMENT '是否调休',
    -- 出差特有字段
    Destination VARCHAR(100) COMMENT '目的地',
    Companions VARCHAR(500) COMMENT '随行人员ID列表（JSON格式）',
    Transportation VARCHAR(50) COMMENT '交通工具',
    Budget DECIMAL(10,2) COMMENT '预算金额',
    FOREIGN KEY (EID) REFERENCES employee(EID) ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY (ApproverID) REFERENCES employee(EID) ON DELETE SET NULL ON UPDATE CASCADE,
    INDEX idx_eid (EID),
    INDEX idx_status (Status),
    INDEX idx_type (ApplicationType),
    INDEX idx_created (CreatedAt)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='申请表';
```

#### 2.2.2 视图设计

系统暂未创建视图，所有查询均通过DAO层直接查询表数据。未来可考虑创建以下视图：

1. **员工信息视图（v_employee_info）**：关联员工、部门、职务表，提供完整的员工信息视图。
2. **考勤统计视图（v_attendance_statistics）**：按员工、日期范围统计考勤数据。
3. **申请统计视图（v_application_statistics）**：按员工、类型、状态统计申请数据。

#### 2.2.3 索引设计

**已创建的索引**：

1. **employee表**：
   - `idx_did (DID)`：部门ID索引，用于按部门查询员工
   - `idx_pid (PID)`：职务ID索引，用于按职务查询员工
   - `idx_role (Role)`：角色索引，用于按角色查询员工

2. **attendance_record表**：
   - `idx_eid (EID)`：员工ID索引，用于查询员工的考勤记录
   - `idx_date (RecordDate)`：日期索引，用于按日期范围查询
   - `uk_employee_date (EID, RecordDate)`：唯一索引，确保每个员工每天只有一条记录

3. **application表**：
   - `idx_eid (EID)`：员工ID索引，用于查询员工的申请记录
   - `idx_status (Status)`：状态索引，用于按状态筛选申请
   - `idx_type (ApplicationType)`：类型索引，用于按类型筛选申请
   - `idx_created (CreatedAt)`：创建时间索引，用于按时间排序

**索引优化说明**：
- 所有外键字段均创建索引，提高关联查询性能。
- 常用查询字段（如状态、类型、日期）创建索引，加快筛选速度。
- 唯一索引保证数据完整性，避免重复数据。

#### 2.2.4 范式分析

**第一范式（1NF）**：所有表都满足1NF，每个字段都是原子值，不可再分。

**第二范式（2NF）**：所有表都满足2NF，非主键字段完全依赖于主键。
- `employee`表：所有字段都依赖于主键EID。
- `attendance_record`表：所有字段都依赖于主键AID。
- `application`表：所有字段都依赖于主键AID。

**第三范式（3NF）**：所有表都满足3NF，非主键字段不传递依赖于主键。
- `employee`表：DID和PID是外键，但它们是独立的实体，不传递依赖。
- `attendance_record`表：EID是外键，不传递依赖。
- `application`表：EID和ApproverID是外键，不传递依赖。

**BCNF（Boyce-Codd范式）**：所有表都满足BCNF，每个决定因素都是候选键。

**结论**：数据库设计满足BCNF范式，数据冗余最小，结构合理。

### 2.3 数据库实现

#### 2.3.1 建立数据库

**创建数据库**：

```sql
CREATE DATABASE attendance CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE attendance;
```

**执行初始化脚本**：

系统提供了完整的数据库初始化脚本 `init_database.sql`，包含：
1. 删除已存在的表（按依赖关系倒序）
2. 创建表结构（按依赖关系顺序）
3. 插入示例数据
4. 数据验证查询

执行方式：
```bash
mysql -u root -p attendance < init_database.sql
```

#### 2.3.2 数据载入

**示例数据说明**：

1. **部门数据**：5个部门（技术部、人事部、财务部、市场部、运营部）
2. **职务配置数据**：6种职务（总经理、部门经理、主管、高级员工、普通员工、实习生）
3. **员工数据**：10名员工，包含1名管理员、2名审批人、7名普通员工
4. **考勤记录数据**：最近7天的考勤记录示例
5. **申请数据**：4条申请示例（请假、加班、出差各一条）

**默认账号信息**：
- 管理员：10001 / 123456 (张三)
- 审批人：10002 / 123456 (李四), 10003 / 123456 (王五)
- 员工：10004-10010 / 123456

#### 2.3.3 测试数据

系统提供了测试数据脚本 `test_attendance_data.sql`，可用于生成更多测试数据。

**数据验证查询**：

```sql
-- 验证数据插入情况
SELECT '部门数量' AS '数据项', COUNT(*) AS '数量' FROM department
UNION ALL
SELECT '职务配置数量', COUNT(*) FROM position_config
UNION ALL
SELECT '员工数量', COUNT(*) FROM employee
UNION ALL
SELECT '考勤记录数量', COUNT(*) FROM attendance_record
UNION ALL
SELECT '申请数量', COUNT(*) FROM application;

-- 显示员工信息（包含部门和职务）
SELECT 
    e.EID AS '员工ID',
    e.EName AS '姓名',
    d.DName AS '部门',
    p.PName AS '职务',
    e.Role AS '角色',
    e.Phone AS '电话'
FROM employee e
LEFT JOIN department d ON e.DID = d.DID
LEFT JOIN position_config p ON e.PID = p.PID
ORDER BY e.EID;
```

#### 2.3.4 数据库备份/恢复

**备份数据库**：

```bash
# 备份整个数据库
mysqldump -u root -p attendance > attendance_backup.sql

# 备份到指定文件
mysqldump -u root -p attendance > D:\backup\attendance_20241201.sql
```

**恢复数据库**：

```bash
# 从备份文件恢复
mysql -u root -p attendance < attendance_backup.sql

# 或者先删除数据库再创建并恢复
mysql -u root -p -e "DROP DATABASE IF EXISTS attendance; CREATE DATABASE attendance;"
mysql -u root -p attendance < attendance_backup.sql
```

**备份策略**：
- 定期备份：建议每天备份一次数据库
- 备份位置：备份到其他设备或云存储
- 备份验证：定期验证备份文件的完整性

**恢复测试**：
定期进行恢复测试，确保备份文件可用。

---

## 3. 应用程序设计

### 3.1 系统功能设计

#### 3.1.1 系统架构

本系统采用**前后端分离架构**，后端使用Spring Boot框架提供RESTful API，前端使用React框架构建单页应用。

**架构层次**：
```
前端（React） → HTTP请求 → 后端（Spring Boot） → DAO层 → MySQL数据库
```

**技术栈**：
- **后端**：Spring Boot 3.3.0、Spring JDBC、Spring Security、MySQL Connector
- **前端**：React 18.3.1、TypeScript 5.5.4、React Router、Axios
- **数据库**：MySQL 8.0+

#### 3.1.2 后端架构设计

**分层架构**：
```
Controller层（控制器）
    ↓
Service层（业务逻辑）
    ↓
DAO层（数据访问）
    ↓
Database（数据库）
```

**Controller层**：处理HTTP请求，参数验证，调用Service层，返回JSON响应。
- `LoginController`：用户登录/注册
- `AttendanceController`：打卡相关接口
- `ApplicationController`：申请相关接口
- `ApprovalController`：审批相关接口
- `AdminController`：管理员功能接口
- `EmployeeController`：员工个人信息接口

**Service层**：业务逻辑处理，事务管理。
- `EmployeeService`：员工业务逻辑
- `DepartmentService`：部门业务逻辑
- `PositionConfigService`：职务配置业务逻辑
- `AttendanceService`：考勤业务逻辑
- `ApplicationService`：申请业务逻辑

**DAO层**：数据访问，使用Spring JDBC Template执行SQL。
- `EmployeeDAO` / `EmployeeDAOImpl`：员工数据访问
- `DepartmentDAO` / `DepartmentDAOImpl`：部门数据访问
- `PositionConfigDAO` / `PositionConfigDAOImpl`：职务配置数据访问
- `AttendanceRecordDAO` / `AttendanceRecordDAOImpl`：考勤记录数据访问
- `ApplicationDAO` / `ApplicationDAOImpl`：申请数据访问

### 3.2 系统详细设计

#### 3.2.1 打卡功能详细设计

**功能描述**：员工根据职务配置的标准工作时间，在允许的时间窗口内进行上班打卡和下班打卡。

**业务规则**：

1. **上班打卡限制**：
   - 必须在标准上班时间前30分钟至后30分钟的时间窗口内打卡
   - 每个员工每天只能进行一次上班打卡
   - 打卡时间晚于标准上班时间则标记为"迟到"（LATE）
   - 打卡时间早于或等于标准上班时间则标记为"正常"（NORMAL）

2. **下班打卡限制**：
   - 必须先完成上班打卡才能进行下班打卡
   - 必须在标准下班时间前30分钟至后30分钟的时间窗口内打卡
   - 每个员工每天只能进行一次下班打卡
   - 签退时间早于标准下班时间则标记为"早退"（EARLY_LEAVE）
   - 签退时间晚于或等于标准下班时间且已上班打卡则更新为"正常"（NORMAL）

3. **工作时长计算**：
   - 工作时长 = 签退时间 - 签到时间（小时）
   - 只有完成上下班打卡后才计算工作时长

**算法流程**：

```
上班打卡流程：
1. 获取员工信息
2. 检查员工是否分配职务
3. 获取职务配置（标准上班时间）
4. 计算允许打卡时间窗口（标准时间±30分钟）
5. 检查当前时间是否在窗口内
6. 检查今日是否已上班打卡
7. 创建或更新考勤记录
8. 判断是否迟到，更新状态
9. 保存记录

下班打卡流程：
1. 获取员工信息
2. 检查员工是否分配职务
3. 获取职务配置（标准下班时间）
4. 计算允许打卡时间窗口（标准时间±30分钟）
5. 检查当前时间是否在窗口内
6. 检查今日是否已上班打卡（必须先上班打卡）
7. 检查今日是否已下班打卡
8. 更新考勤记录（签退时间、地点）
9. 计算工作时长
10. 判断是否早退，更新状态
11. 保存记录
```

**伪代码**：

```java
// 上班打卡
public String checkIn(Integer employeeId, String location) {
    // 1. 获取员工信息
    Employee employee = employeeDAO.findByEmployeeId(employeeId);
    if (employee == null) return "员工不存在";
    
    // 2. 获取职务配置
    PositionConfig config = positionConfigDAO.findById(employee.getPid());
    LocalTime workStartTime = config.getWorkStartTime().toLocalTime();
    
    // 3. 计算时间窗口
    LocalTime allowedStart = workStartTime.minusMinutes(30);
    LocalTime allowedEnd = workStartTime.plusMinutes(30);
    LocalTime currentTime = LocalTime.now();
    
    // 4. 验证时间窗口
    if (currentTime.isBefore(allowedStart)) 
        return "未到打卡时间";
    if (currentTime.isAfter(allowedEnd)) 
        return "已错过打卡时间";
    
    // 5. 检查今日是否已打卡
    AttendanceRecord todayRecord = attendanceRecordDAO
        .findByEmployeeIdAndDate(employeeId, LocalDate.now());
    if (todayRecord != null && todayRecord.getCheckInTime() != null)
        return "今日已上班打卡";
    
    // 6. 创建或更新记录
    if (todayRecord == null) {
        todayRecord = new AttendanceRecord();
        todayRecord.setEid(employeeId);
        todayRecord.setRecordDate(LocalDate.now());
    }
    todayRecord.setCheckInTime(LocalDateTime.now());
    todayRecord.setCheckInLocation(location);
    
    // 7. 判断是否迟到
    if (currentTime.isAfter(workStartTime)) {
        todayRecord.setStatus("LATE");
    } else {
        todayRecord.setStatus("NORMAL");
    }
    
    // 8. 保存
    if (todayRecord.getAid() == null) {
        attendanceRecordDAO.insert(todayRecord);
    } else {
        attendanceRecordDAO.update(todayRecord);
    }
    
    return "上班打卡成功";
}

// 下班打卡
public String checkOut(Integer employeeId, String location) {
    // 1-3. 同上班打卡
    // ...
    
    // 4. 检查是否已上班打卡
    AttendanceRecord todayRecord = attendanceRecordDAO
        .findByEmployeeIdAndDate(employeeId, LocalDate.now());
    if (todayRecord == null || todayRecord.getCheckInTime() == null)
        return "请先完成上班打卡";
    if (todayRecord.getCheckOutTime() != null)
        return "今日已下班打卡";
    
    // 5. 更新签退信息
    todayRecord.setCheckOutTime(LocalDateTime.now());
    todayRecord.setCheckOutLocation(location);
    
    // 6. 计算工作时长
    Duration duration = Duration.between(
        todayRecord.getCheckInTime(), 
        todayRecord.getCheckOutTime()
    );
    todayRecord.setWorkHours(duration.toHours() + duration.toMinutesPart() / 60.0);
    
    // 7. 判断是否早退
    LocalTime workEndTime = config.getWorkEndTime().toLocalTime();
    if (LocalTime.now().isBefore(workEndTime)) {
        todayRecord.setStatus("EARLY_LEAVE");
    } else {
        todayRecord.setStatus("NORMAL");
    }
    
    // 8. 保存
    attendanceRecordDAO.update(todayRecord);
    return "下班打卡成功";
}
```

#### 3.2.2 申请功能详细设计

**功能描述**：员工可以提交请假、加班、出差、补卡四种类型的申请，系统对申请进行验证和限制。

**业务规则**：

**1. 请假申请限制**：
- 开始时间不能早于当前时间
- 结束时间必须晚于开始时间
- 理由必填
- 申请时间段不能与其他已通过或待审批的申请重叠
- 支持多种请假类型：年假、病假、事假、婚假、产假、其他
- 可上传附件（图片/PDF）

**2. 加班申请限制**：
- 开始时间不能早于当前时间
- 结束时间必须晚于开始时间
- 理由必填
- 申请时间段不能与其他已通过或待审批的申请重叠
- 支持加班类型：平日加班、周末加班、节假日加班
- 可选择是否调休

**3. 出差申请限制**：
- 开始时间不能早于当前时间
- 结束时间必须晚于开始时间
- 理由必填
- 出差地点必填，最多50字
- 申请时间段不能与其他已通过或待审批的申请重叠
- 可填写随行人员、交通工具、费用预算

**4. 补卡申请限制**：
- **补卡次数限制**：每月最多3次补卡机会
- **时间限制**：补卡时间不能晚于当前时间
- **重复限制**：补卡日期不能与正常考勤日期相同（已有NORMAL状态的考勤记录）
- **重复提交限制**：同一天不能重复提交补卡申请（已有PENDING或APPROVED状态的补卡申请）
- 理由必填

**算法流程**：

```
提交申请流程：
1. 获取申请类型和员工ID
2. 根据申请类型进行不同验证
   - 补卡申请：
     a. 检查补卡次数限制（每月最多3次）
     b. 检查补卡时间不能晚于当前时间
     c. 检查补卡日期不能与正常考勤重复
     d. 检查同一天不能重复提交补卡申请
   - 其他申请：
     a. 检查开始时间不能早于当前时间
     b. 检查结束时间必须晚于开始时间
     c. 检查申请时间段不能与其他申请重叠
3. 创建申请记录，状态设为PENDING
4. 保存申请
```

**伪代码**：

```java
public Application submitApplication(Application application) {
    String type = application.getApplicationType();
    Integer employeeId = application.getEid();
    
    // 补卡申请特殊验证
    if ("REISSUE".equals(type)) {
        LocalDateTime reissueTime = application.getReissueTime();
        LocalDate reissueDate = reissueTime.toLocalDate();
        
        // 1. 检查补卡次数限制
        int currentMonthCount = getReissueLimit(employeeId);
        if (currentMonthCount >= 3) {
            throw new RuntimeException("本月补卡次数已达上限（3次）");
        }
        
        // 2. 检查补卡时间不能晚于当前时间
        if (reissueTime.isAfter(LocalDateTime.now())) {
            throw new RuntimeException("补卡时间不能晚于当前时间");
        }
        
        // 3. 检查补卡日期不能与正常考勤重复
        AttendanceRecord existingRecord = attendanceRecordDAO
            .findByEmployeeIdAndDate(employeeId, reissueDate);
        if (existingRecord != null && "NORMAL".equals(existingRecord.getStatus())) {
            throw new RuntimeException("该日期已有正常考勤记录，不能提交补卡申请");
        }
        
        // 4. 检查同一天不能重复提交补卡申请
        List<Application> existingReissues = applicationDAO.findByEmployeeId(employeeId);
        boolean hasDuplicate = existingReissues.stream()
            .filter(app -> "REISSUE".equals(app.getApplicationType()))
            .filter(app -> "PENDING".equals(app.getStatus()) || 
                          "APPROVED".equals(app.getStatus()))
            .anyMatch(app -> {
                if (app.getReissueTime() != null) {
                    return app.getReissueTime().toLocalDate().equals(reissueDate);
                }
                return false;
            });
        if (hasDuplicate) {
            throw new RuntimeException("该日期的补卡申请已存在，不能重复提交");
        }
    } else {
        // 其他申请验证
        LocalDateTime startTime = application.getStartTime();
        LocalDateTime endTime = application.getEndTime();
        
        // 1. 检查开始时间不能早于当前时间
        if (startTime.isBefore(LocalDateTime.now())) {
            throw new RuntimeException("开始时间不能早于当前时间");
        }
        
        // 2. 检查结束时间必须晚于开始时间
        if (!endTime.isAfter(startTime)) {
            throw new RuntimeException("结束时间必须晚于开始时间");
        }
        
        // 3. 检查申请时间段不能与其他申请重叠
        List<Application> existingApps = applicationDAO.findByEmployeeId(employeeId);
        boolean hasOverlap = existingApps.stream()
            .filter(app -> "PENDING".equals(app.getStatus()) || 
                          "APPROVED".equals(app.getStatus()))
            .anyMatch(app -> {
                // 检查时间段是否重叠
                return !(endTime.isBefore(app.getStartTime()) || 
                        startTime.isAfter(app.getEndTime()));
            });
        if (hasOverlap) {
            throw new RuntimeException("申请时间段与其他申请重叠，不能重复提交");
        }
    }
    
    // 设置默认状态
    application.setStatus("PENDING");
    application.setCreatedAt(LocalDateTime.now());
    
    // 保存申请
    return applicationDAO.insert(application);
}
```

#### 3.2.3 审批功能详细设计

**功能描述**：审批人和管理员可以对待审批的申请进行审批操作，包括通过和驳回。

**业务规则**：

1. **审批权限**：
   - 审批人（APPROVER）可以审批所有待审批的申请
   - 管理员（ADMIN）可以审批所有申请（包括已审批的申请）

2. **审批限制**：
   - 只能审批状态为"PENDING"（待审批）的申请
   - 审批通过后，申请状态更新为"APPROVED"，记录审批人和审批时间
   - 审批驳回后，申请状态更新为"REJECTED"，记录审批人、审批时间和拒绝原因
   - 驳回时必须填写拒绝原因

3. **补卡申请特殊处理**：
   - 补卡申请审批通过后，如果该日期没有考勤记录，则自动创建考勤记录
   - 如果该日期已有考勤记录，则更新考勤记录

**算法流程**：

```
审批流程：
1. 获取申请记录
2. 检查申请是否存在
3. 检查申请状态是否为PENDING
4. 根据审批结果：
   - 通过：
     a. 更新申请状态为APPROVED
     b. 记录审批人和审批时间
     c. 如果是补卡申请，创建或更新考勤记录
   - 驳回：
     a. 更新申请状态为REJECTED
     b. 记录审批人、审批时间和拒绝原因
5. 保存申请记录
```

**伪代码**：

```java
public Application approveApplication(Integer applicationId, Integer approverId) {
    // 1. 获取申请记录
    Application application = applicationDAO.findById(applicationId);
    if (application == null) {
        throw new RuntimeException("申请不存在");
    }
    
    // 2. 检查申请状态
    if (!"PENDING".equals(application.getStatus())) {
        throw new RuntimeException("该申请已处理，不能重复审批");
    }
    
    // 3. 更新申请状态
    application.setStatus("APPROVED");
    application.setApproverId(approverId);
    application.setApproveTime(LocalDateTime.now());
    
    // 4. 如果是补卡申请，创建或更新考勤记录
    if ("REISSUE".equals(application.getApplicationType())) {
        handleReissueApproval(application);
    }
    
    // 5. 保存
    applicationDAO.update(application);
    return application;
}

public Application rejectApplication(Integer applicationId, Integer approverId, String reason) {
    // 1-2. 同审批通过
    // ...
    
    // 3. 更新申请状态
    application.setStatus("REJECTED");
    application.setApproverId(approverId);
    application.setApproveTime(LocalDateTime.now());
    application.setRejectReason(reason);
    
    // 4. 保存
    applicationDAO.update(application);
    return application;
}

private void handleReissueApproval(Application application) {
    LocalDate reissueDate = application.getReissueTime().toLocalDate();
    Integer employeeId = application.getEid();
    
    // 检查是否已有考勤记录
    AttendanceRecord record = attendanceRecordDAO
        .findByEmployeeIdAndDate(employeeId, reissueDate);
    
    if (record == null) {
        // 创建新记录
        record = new AttendanceRecord();
        record.setEid(employeeId);
        record.setRecordDate(reissueDate);
        record.setStatus("NORMAL");
        
        // 根据补卡类型设置打卡时间
        if ("MISSING_CHECK_IN".equals(application.getReissueType())) {
            // 补上班打卡，使用标准下班时间作为签退时间
            PositionConfig config = getPositionConfig(employeeId);
            record.setCheckInTime(application.getReissueTime());
            record.setCheckOutTime(LocalDateTime.of(reissueDate, 
                config.getWorkEndTime().toLocalTime()));
        } else if ("MISSING_CHECK_OUT".equals(application.getReissueType())) {
            // 补下班打卡，使用标准上班时间作为签到时间
            PositionConfig config = getPositionConfig(employeeId);
            record.setCheckInTime(LocalDateTime.of(reissueDate, 
                config.getWorkStartTime().toLocalTime()));
            record.setCheckOutTime(application.getReissueTime());
        }
        
        attendanceRecordDAO.insert(record);
    } else {
        // 更新现有记录
        if ("MISSING_CHECK_IN".equals(application.getReissueType())) {
            record.setCheckInTime(application.getReissueTime());
        } else if ("MISSING_CHECK_OUT".equals(application.getReissueType())) {
            record.setCheckOutTime(application.getReissueTime());
        }
        record.setStatus("NORMAL");
        attendanceRecordDAO.update(record);
    }
}
```

#### 3.2.4 管理员功能详细设计

**功能描述**：管理员拥有系统的完整管理权限，包括员工管理、部门管理、职务管理、考勤汇总和审批管理。

**业务规则**：

**1. 员工管理限制**：

- **新增员工**：
  - 必须填写姓名、部门ID、职务ID
  - 手机号必须唯一（可为空，为空时不违反UNIQUE约束）
  - 默认密码为"123456"
  - 默认角色为"EMPLOYEE"
  - 必须验证部门和职务是否存在
  - 部门ID和职务ID必须大于0

- **编辑员工**：
  - 可修改姓名、手机号、部门、职务、角色
  - 不能修改密码（需通过重置密码功能）
  - 修改部门或职务时必须验证新部门/职务是否存在
  - 手机号修改时仍需保证唯一性

- **删除员工**：
  - 删除员工会级联删除该员工的所有考勤记录（ON DELETE CASCADE）
  - 删除员工会级联删除该员工的所有申请记录（ON DELETE CASCADE）
  - 如果该员工是部门负责人，删除后部门负责人字段设置为NULL

- **重置密码**：
  - 将员工密码重置为默认密码"123456"

**2. 部门管理限制**：

- **新增部门**：
  - 自动生成部门ID（格式：D000000001，递增）
  - 必须填写部门名称
  - 负责人ID可为空

- **编辑部门**：
  - 可修改部门名称、描述、负责人
  - 负责人ID必须对应存在的员工ID

- **删除部门**：
  - **重要限制**：如果部门下还有员工，则不能删除（外键约束：ON DELETE RESTRICT）
  - 删除前必须检查：`SELECT COUNT(*) FROM employee WHERE DID = ?`
  - 如果有员工，返回错误："该部门下还有员工，无法删除。请先将员工转移到其他部门或删除员工后再删除部门。"

**3. 职务管理限制**：

- **新增职务**：
  - 自动生成职务ID（格式：P000000001，递增）
  - 必须填写职务名称、上班时间、下班时间
  - 默认月工作日21天，每日工作时长8小时

- **编辑职务**：
  - 可修改所有字段
  - 修改工作时间会影响使用该职务的所有员工的打卡时间窗口

- **删除职务**：
  - **重要限制**：如果职务下还有员工，则不能删除（外键约束：ON DELETE RESTRICT）
  - 删除前必须检查：`SELECT COUNT(*) FROM employee WHERE PID = ?`
  - 如果有员工，返回错误："该职务下还有员工，无法删除。请先将员工转移到其他职务或删除员工后再删除职务。"

**算法流程**：

```
删除部门流程：
1. 检查部门是否存在
2. 检查部门下是否还有员工
   SELECT COUNT(*) FROM employee WHERE DID = ?
3. 如果有员工：
   返回错误："该部门下还有员工，无法删除"
4. 如果没有员工：
   执行删除操作

删除职务流程：
1. 检查职务是否存在
2. 检查职务下是否还有员工
   SELECT COUNT(*) FROM employee WHERE PID = ?
3. 如果有员工：
   返回错误："该职务下还有员工，无法删除"
4. 如果没有员工：
   执行删除操作
```

**伪代码**：

```java
// 删除部门
public ResponseEntity<Map<String, Object>> deleteDepartment(String id) {
    // 1. 检查部门是否存在
    Department department = departmentService.getDepartmentById(id);
    if (department == null) {
        return ResponseEntity.status(404).body(error("部门不存在"));
    }
    
    // 2. 检查部门下是否还有员工
    if (employeeService.hasEmployeesInDepartment(id)) {
        return ResponseEntity.status(400).body(error(
            "该部门下还有员工，无法删除。请先将员工转移到其他部门或删除员工后再删除部门。"
        ));
    }
    
    // 3. 执行删除
    departmentService.deleteDepartment(id);
    return ResponseEntity.ok(success("部门删除成功"));
}

// 删除职务
public ResponseEntity<Map<String, Object>> deletePositionConfig(String id) {
    // 1. 检查职务是否存在
    PositionConfig positionConfig = positionConfigService.getPositionConfigById(id);
    if (positionConfig == null) {
        return ResponseEntity.status(404).body(error("职务配置不存在"));
    }
    
    // 2. 检查职务下是否还有员工
    if (employeeService.hasEmployeesInPosition(id)) {
        return ResponseEntity.status(400).body(error(
            "该职务下还有员工，无法删除。请先将员工转移到其他职务或删除员工后再删除职务。"
        ));
    }
    
    // 3. 执行删除
    positionConfigService.deletePositionConfig(id);
    return ResponseEntity.ok(success("职务配置删除成功"));
}

// 检查部门下是否有员工
public boolean hasEmployeesInDepartment(String departmentId) {
    List<Employee> employees = employeeDAO.findByDepartmentId(
        Integer.parseInt(departmentId.replaceAll("[^0-9]", ""))
    );
    return employees != null && !employees.isEmpty();
}

// 检查职务下是否有员工
public boolean hasEmployeesInPosition(String positionId) {
    // 需要通过员工DAO查询使用该职务的员工
    List<Employee> allEmployees = employeeDAO.findAll();
    return allEmployees.stream()
        .anyMatch(emp -> positionId.equals(emp.getPid()));
}
```

### 3.3 小结

#### 3.3.1 主界面截图

**说明**：由于无法直接生成截图，此处描述主界面布局：

**首页（HomePage）布局**：
```
┌─────────────────────────────────────────────┐
│  考勤管理系统          [菜单]  [用户名] [退出] │
├─────────────────────────────────────────────┤
│                                             │
│  欢迎，[用户名]                              │
│  当前时间：[实时时间显示]                     │
│                                             │
│  ┌───────────────────────────────────────┐ │
│  │           打卡功能                     │ │
│  │                                        │ │
│  │   [上班打卡/下班打卡按钮]              │ │
│  │   位置：[GPS位置信息]                  │ │
│  │   状态：[打卡状态提示]                  │ │
│  └───────────────────────────────────────┘ │
│                                             │
│  ┌───────────────────────────────────────┐ │
│  │        今日打卡记录                    │ │
│  │                                        │ │
│  │   上班打卡：[时间] [位置]              │ │
│  │   下班打卡：[时间] [位置]              │ │
│  │   状态：[正常/迟到/早退/缺卡]          │ │
│  └───────────────────────────────────────┘ │
│                                             │
└─────────────────────────────────────────────┘
```

#### 3.3.2 算法框图

**打卡时间判断算法流程图**：

```
开始
  ↓
获取职务配置
  ↓
是否有职务配置？
  ├─ 否 → 显示"加载职务配置中..."，按钮禁用
  └─ 是 → 继续
  ↓
获取今日打卡记录
  ↓
是否有打卡记录？
  ├─ 否 → 判断上班打卡时间窗口
  │        ├─ 在窗口内 → 显示"上班打卡"，按钮启用
  │        ├─ 早于窗口 → 显示"未到打卡时间"，按钮禁用
  │        └─ 晚于窗口 → 显示"已错过打卡时间"，按钮禁用
  │
  └─ 是 → 检查打卡状态
          ├─ 已上班打卡 + 已下班打卡 → 显示"今日已打卡完成"，按钮禁用
          │
          └─ 已上班打卡 + 未下班打卡 → 判断下班打卡时间窗口
                    ├─ 在窗口内 → 显示"下班打卡"，按钮启用
                    ├─ 早于窗口 → 显示"未到下班打卡时间"，按钮禁用
                    └─ 晚于窗口 → 显示"已错过下班打卡时间"，按钮禁用
  ↓
结束
```

**申请提交验证算法流程图**：

```
开始
  ↓
用户提交申请表单
  ↓
获取申请类型
  ↓
申请类型是什么？
  ├─ 请假(LEAVE) → 验证请假字段
  │                 ├─ 开始时间、结束时间必填
  │                 ├─ 开始时间不能早于当前时间
  │                 ├─ 结束时间必须晚于开始时间
  │                 └─ 理由必填
  │
  ├─ 加班(OVERTIME) → 验证加班字段
  │                   ├─ 开始时间、结束时间必填
  │                   ├─ 开始时间不能早于当前时间
  │                   ├─ 结束时间必须晚于开始时间
  │                   └─ 理由必填
  │
  ├─ 出差(BUSINESS_TRIP) → 验证出差字段
  │                        ├─ 开始时间、结束时间必填
  │                        ├─ 开始时间不能早于当前时间
  │                        ├─ 结束时间必须晚于开始时间
  │                        ├─ 出差地点必填（最多50字）
  │                        └─ 理由必填
  │
  └─ 补卡(REISSUE) → 验证补卡字段
                     ├─ 补卡时间必填
                     ├─ 补卡时间不能晚于当前时间
                     ├─ 检查补卡次数限制（每月最多3次）
                     ├─ 检查补卡日期不能与正常考勤重复
                     └─ 检查同一天不能重复提交补卡申请
  ↓
所有验证通过？
  ├─ 否 → 显示错误信息，阻止提交
  └─ 是 → 构造请求数据，调用API提交
  ↓
结束
```

#### 3.3.3 伪代码

**打卡时间判断伪代码**：

```typescript
function getButtonState() {
  // 1. 检查职务配置
  if (!positionConfig) {
    if (hasErrorMessage) {
      return { text: errorMessage, disabled: true, type: null }
    }
    return { text: '加载职务配置中...', disabled: true, type: null }
  }

  // 2. 检查今日打卡记录
  if (todayRecord) {
    const hasCheckIn = !!todayRecord.checkInTime
    const hasCheckOut = !!todayRecord.checkOutTime

    // 2.1 已完成打卡
    if (hasCheckIn && hasCheckOut) {
      return { text: '今日已打卡完成', disabled: true, type: null }
    }

    // 2.2 已上班打卡，等待下班打卡
    if (hasCheckIn && !hasCheckOut) {
      const currentTime = getCurrentTime() // HH:MM格式
      const workEndTime = positionConfig.workEndTime // HH:MM格式
      const allowedStartTime = subtractMinutes(workEndTime, 30)
      const allowedEndTime = addMinutes(workEndTime, 30)

      if (isTimeInRange(currentTime, allowedStartTime, allowedEndTime)) {
        return { text: '下班打卡', disabled: false, type: 'CHECK_OUT' }
      }
      if (isTimeBefore(currentTime, allowedStartTime)) {
        return { text: `未到下班打卡时间（${allowedStartTime}开始）`, disabled: true, type: null }
      }
      return { text: `已错过下班打卡时间（最晚${allowedEndTime}）`, disabled: true, type: null }
    }
  }

  // 3. 未上班打卡，判断上班打卡时间窗口
  const currentTime = getCurrentTime()
  const workStartTime = positionConfig.workStartTime
  const allowedStartTime = subtractMinutes(workStartTime, 30)
  const allowedEndTime = addMinutes(workStartTime, 30)

  if (isTimeInRange(currentTime, allowedStartTime, allowedEndTime)) {
    return { text: '上班打卡', disabled: false, type: 'CHECK_IN' }
  }
  if (isTimeBefore(currentTime, allowedStartTime)) {
    return { text: `未到打卡时间（${allowedStartTime}开始）`, disabled: true, type: null }
  }
  return { text: `已错过上班打卡时间（最晚${allowedEndTime}）`, disabled: true, type: null }
}
```

**申请验证伪代码**：

```typescript
function validateApplicationForm(type, formData) {
  const errors = []

  // 通用验证：理由必填
  if (!formData.reason || !formData.reason.trim()) {
    errors.push('请填写申请理由')
    return errors
  }

  switch (type) {
    case 'LEAVE':
    case 'OVERTIME':
    case 'BUSINESS_TRIP':
      // 验证开始时间和结束时间
      if (!formData.startTime || !formData.endTime) {
        errors.push('请填写完整的时间信息')
        return errors
      }

      const now = new Date()
      const startTime = new Date(formData.startTime)
      const endTime = new Date(formData.endTime)

      if (startTime < now) {
        errors.push('开始时间不能早于当前时间')
      }

      if (startTime >= endTime) {
        errors.push('结束时间必须晚于开始时间')
      }

      // 出差特殊验证
      if (type === 'BUSINESS_TRIP') {
        if (!formData.destination || !formData.destination.trim()) {
          errors.push('请填写出差地点')
        }
        if (formData.destination && formData.destination.length > 50) {
          errors.push('出差地点不能超过50字')
        }
      }
      break

    case 'REISSUE':
      // 验证补卡时间
      if (!formData.reissueTime) {
        errors.push('请选择补卡时间')
        return errors
      }

      const reissueTime = new Date(formData.reissueTime)
      if (reissueTime > now) {
        errors.push('补卡时间不能晚于当前时间')
      }

      // 验证补卡次数限制
      if (formData.reissueLimit) {
        if (formData.reissueLimit.currentMonth >= formData.reissueLimit.limit) {
          errors.push(`本月补卡次数已达上限（${formData.reissueLimit.limit}次）`)
        }
      }
      break
  }

  return errors
}
```

**统计信息计算伪代码**：

```typescript
function calculateStatistics(records: AttendanceRecord[]) {
  if (!records || records.length === 0) {
    return {
      normalDays: 0,
      lateDays: 0,
      earlyLeaveDays: 0,
      missingDays: 0,
      totalWorkHours: 0,
      totalDays: 0
    }
  }

  let normalDays = 0
  let lateDays = 0
  let earlyLeaveDays = 0
  let missingDays = 0
  let totalWorkHours = 0

  records.forEach(record => {
    // 根据状态分类统计
    switch (record.status) {
      case 'NORMAL':
        normalDays++
        break
      case 'LATE':
        lateDays++
        break
      case 'EARLY_LEAVE':
        earlyLeaveDays++
        break
      case 'MISSING':
        missingDays++
        break
    }

    // 累加工作时长
    if (record.workHours) {
      totalWorkHours += record.workHours
    }
  })

  return {
    normalDays,
    lateDays,
    earlyLeaveDays,
    missingDays,
    totalWorkHours,
    totalDays: records.length
  }
}
```

---

## 总结

本实验报告详细介绍了考勤管理系统的需求分析、数据库设计和应用程序设计。系统采用前后端分离架构，使用Spring Boot和React技术栈，实现了完整的考勤管理功能，包括打卡、申请、审批、统计等核心功能。数据库设计满足BCNF范式，具有良好的数据完整性和扩展性。系统实现了完善的业务规则和限制，确保数据的准确性和一致性。

